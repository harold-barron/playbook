/*
    import MyPokemon from './pokemon.js'
*/

export default class 
{
    constructor(name)
    {
        this.name = name
    }

    sayHello(message)
    {
        console.log(`${this.name} says ${message}`)
    }
}